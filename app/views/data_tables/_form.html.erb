<%= form_with(model: data_table, local: true) do |form| %>
  <% if data_table.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(data_table.errors.count, "error") %> prohibited this data_table from being saved:</h2>

      <ul>
      <% data_table.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
